package sd.grupo1;

/**
 * Unit test for simple App.
 */
public class AppTest {
    /**
     * // * Rigorous Test :-)
     * //
     */
    // @Test
    // public void ConvertUsertoJson() {
    // User u = new User("Pool", "Luque", "73057755");
    // Account account = new Account(u);
    // account.setAcc_bal(1000.0);

    // Account account2 = new Account(u);
    // account2.setAcc_bal(2000.0);

    // User u2 = new User("PAul", "Luque", "73057755");
    // Account account3 = new Account(u);
    // account3.setAcc_bal(1000.0);

    // List<User> users = new ArrayList<User>();
    // users.add(u);
    // users.add(u2);

    // Gson gson = new GsonBuilder().setPrettyPrinting().create();
    // String json = gson.toJson(users);

    // // Gson gson = new Gson();
    // // String jsonString = gson.toJson(users);

    // // System.out.println(json);

    // // Deserializar objeto: convertir la string JSON de nuevo en objeto `Person`
    // List<User> personList = null;

    // personList = Arrays.asList(gson.fromJson(json, User[].class));

    // System.out.println("\nConverting JSON string to Person object:\n"
    // + personList.toString());

    // }

    // @Test
    // public void ConvertAccounttoJson() {
    // User u = new User("Pool", "Luque", "73057755");
    // u.setId(12);
    // Account account = new Account(u);
    // account.setAcc_bal(1000.0);
    // System.out.println(u);

    // System.out.println(account);

    // Gson gson = new Gson();
    // String jsonString = gson.toJson(account);

    // System.out.println(jsonString);

    // // Deserializar objeto: convertir la string JSON de nuevo en objeto `Person`
    // Account p = gson.fromJson(jsonString, Account.class);

    // System.out.println("\nConverting JSON string to Account object:\n"
    // + p.toString());

    // }
}
